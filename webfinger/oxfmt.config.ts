import { defineConfig } from 'oxfmt';

export default defineConfig({
  arrowParens: 'always',
  experimentalSortPackageJson: false,
  printWidth: 120,
  proseWrap: 'always',
  singleQuote: true,
  trailingComma: 'all',
});
